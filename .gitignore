node_modules/
dist/
*.tsbuildinfo
# CLI-era output dirs — anchored to repo root so they don't swallow source
# dirs like extension/src/capture/.
/capture/
/rendered/
/output.*
.demoscope-capture/
.DS_Store
